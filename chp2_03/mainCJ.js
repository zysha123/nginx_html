const math = require('./mainJS_math.js');

const x = 10;
const y = 5;

console.log(math.sum(x, y))
console.log(math.difference(x, y))
console.log(math.product(x, y))
console.log(math.quotient(x, y))